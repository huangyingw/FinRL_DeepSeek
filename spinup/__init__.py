# Modified spinup - minimal imports for CPPO training
# Only import utils (no algorithm imports to avoid gym dependency)

# Loggers
from spinup.utils.logx import Logger, EpochLogger

# Version
from spinup.version import __version__
